apiVersion: v1
kind: Service

metadata:
  name: asset-service
  namespace: ops-platform
  labels:
    app: asset-service

spec:
  selector:
    app: asset-service

  ports:
    - name: http
      port: 8000
      targetPort: 8000

  type: ClusterIP