apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-data
  namespace: ops-platform
  labels:
    app: postgres

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi