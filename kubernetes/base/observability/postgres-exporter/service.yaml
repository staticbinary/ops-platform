apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: ops-platform
  labels:
    app: postgres-exporter
    component: observability
spec:
  type: ClusterIP
  selector:
    app: postgres-exporter
  ports:
    - name: metrics
      port: 9187
      targetPort: metrics
      protocol: TCP
