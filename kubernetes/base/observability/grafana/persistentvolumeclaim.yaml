apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: ops-platform
  labels:
    app: grafana
    component: observability
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
