apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: ops-platform
  labels:
    app: loki
    component: observability
spec:
  type: ClusterIP

  selector:
    app: loki

  ports:
    - name: http
      port: 3100
      targetPort: http
      protocol: TCP

    - name: grpc
      port: 9096
      targetPort: grpc
      protocol: TCP
