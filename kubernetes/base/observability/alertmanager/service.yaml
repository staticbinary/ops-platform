apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: ops-platform
  labels:
    app: alertmanager
    component: observability
spec:
  type: ClusterIP

  selector:
    app: alertmanager

  ports:
    - name: http
      port: 9093
      targetPort: http
      protocol: TCP
