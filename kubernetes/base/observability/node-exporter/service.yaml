apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: ops-platform
  labels:
    app: node-exporter
    component: observability
spec:
  type: ClusterIP
  selector:
    app: node-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
      protocol: TCP
